{
  "name": "behelith-coop-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Co-op hunt server for Behel'ith. Deploys on its own; the game stays a dependency-free static site.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node protocol_test.mjs && node session_test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
